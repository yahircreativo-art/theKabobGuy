---
import Button from '../components/Button.astro';


interface Props {
  title: string;
  description: string;
  price?: string;
  button?: {
    label: string;
    href: string;
    };
    backgroundImage?: {
      src: string;
      alt: string;
    }
}
const {
    title,
    description,
    button,
    backgroundImage,
    price
} = Astro.props;
---

<div class="flex flex-col border border-zinc-900 overflow-hidden h-full shadow-lg 

card-fade opacity-0 translate-y-8 transition-all duration-1000 ease-out">
    <img
        src={backgroundImage?.src}
        alt={backgroundImage?.alt}
        class="w-full h-fit object-cover object-center"
    />
    <div class="flex flex-col gap-4 p-4 bg-zinc-950">

        <div class="flex flex-row gap-4 items-center justify-between">
            <h2 class="text-amber-300 text-xl font-medium">{title}</h2>
            <span class="text-zinc-900 px-2 text-2xl font-bold bg-amber-300">{price}</span>
        </div>
        <p class="text-zinc-300 font-light">{description}</p>
        <div class="flex flex-row">
            {button && (
                <Button label={button.label} href={button.href} />
            )}
        </div>
    </div>
</div>